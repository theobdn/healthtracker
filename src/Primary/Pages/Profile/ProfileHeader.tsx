import React from 'react';
import {Box} from "@mui/material";

const ProfileHeader = () => {
    return (
        <Box>
            Profile header
        </Box>
    );
};

export default ProfileHeader;